import { CurrentUser } from '../../shared/models/current-user.model';

export const mockCurrentUser: CurrentUser = {
  token: 'token',
  firstname: 'first',
  email: 'email',
  userId: 'userId',
  expiresAt: 100,
};
